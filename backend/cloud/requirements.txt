# Kobevoice Cloud control-plane dependencies.
# Deliberately lightweight — no torch/ML deps — so the SaaS API can run and
# scale independently of the GPU voice engine.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
bcrypt>=4.1
python-jose[cryptography]>=3.3
python-multipart>=0.0.9
email-validator>=2.1
httpx>=0.27
stripe>=9.0
# Postgres driver for production (optional in dev/SQLite):
psycopg2-binary>=2.9
